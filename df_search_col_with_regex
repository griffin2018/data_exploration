def search_col_with_regex(df, patterns):
    '''Applies different regex patterns with a capturing group to identify if regex covers all scenarios
    patterns can be a list of patterns or a single pattern, previous regex compilation mandatory
    Call this along with the apply method on the dataframe
    USAGE as: dataframe.column_name.apply(func_name, patterns)
    No data validations are being performed, GIGO in place'''

    for pattern in patterns:
        m = re.search(pattern, df)
        if m:
            return m.group(1)
            break
    return df
